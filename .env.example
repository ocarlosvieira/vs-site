# Copie para .env.local (desenvolvimento) ou defina no Portainer (produção).
# O .env / .env.* está no .gitignore e nunca vai para o repositório.

# Destino do formulário de contato do site.
# Se não for definido, o código usa esta mesma URL como padrão.
N8N_WEBHOOK_URL=https://n8n.vsgrowth.com.br/webhook/form-site-vs
